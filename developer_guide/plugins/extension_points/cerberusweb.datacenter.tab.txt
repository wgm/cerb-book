cerberusweb.datacenter.tab
--------------------------

The **cerberusweb.datacenter.tab** extension point allows you to create new tabs on the ``Datacenter`` page.

Plugin Manifest
===============

In :doc:`plugin.xml </developer_guide/plugins/plugin.xml>`:

.. code-block:: xml
    
    <!-- Activity Tab -->
    <extension point="cerberusweb.datacenter.tab">
        <id>example.datacenter.tab</id>
        <name>Example Datacenter Tab</name>
        <class>
            <file>api/App.php</file>
            <name>ExampleDatacenterTab</name>
        </class>
        <params>
            <param key="uri" value="example" />
            <param key="title" value="example.datacenter.tab" />
        </params>
    </extension>

**<param key="uri" value="example">**
    Binds the tab to the ``example`` url.

**<param key="title" value="example.title">**
    Sets the text of the tab. Devblocks will automatically pass this string through the :doc:`translation service` before rendering it, so you should use a namespaced translation string whenever possible. You can use literal text if you wish, though we advise against it.

Implementation
==============
.. code-block:: php

    <?php
    class ExampleDatacenterTab
        extends Extension_DatacenterTab {
            
            function showTab() {}
            
            function saveTab() {}
    };

**showTab()**
    This method is called when a worker visits your tab on the Datacenter page.

Examples
========

Domains tab
~~~~~~~~~~~

:doc:`plugin.xml </developer_guide/plugins/plugin.xml>`:
    https://github.com/cerb5-plugins/cerberusweb.datacenter.domains/blob/5.7/plugin.xml#L134
Implementation:
    https://github.com/cerb5-plugins/cerberusweb.datacenter.domains/blob/5.7/api/App.php#L2